package com.example.lolapp.repository;

import com.example.lolapp.model.CombatStyle;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CombatStyleRepository extends JpaRepository<CombatStyle, Long> {
}